const OUTREACH_BASE = ({ creatorName, brandName, pitch }) => `
    Hi ${brandName} Team,
    ${pitch}
    Best,
    ${creatorName}
    —
    Sent with ManAIger — Your AI Manager for Streamers and Creators.
`;
module.exports = { OUTREACH_BASE };